JAVA核心知识
=
JVM
-
**Hotspot JVM中的线程与原生操作系统线程有直接的映射关系**  

**虚拟机栈**  
**本地方法区**  
**堆**：创建的对象和数组  
**方法区**：存储JVM类变量，常量，静态变量，即时编译器编译后的代码，运行时常量池  
**程序计数器**  
 
**新生代1/3**MinorGC采用复制算法  
**老年代2/3**FullGC采用标记清除算法
  
**引用计数法**
**根搜索算法**  
**标记清除算法**  
**复制算法**  
**标记整理算法**  
**分代收集算法**：新生代与复制算法，老年代与标记整理算法  

**强引用**不可能被垃圾回收机制回收
**软引用**系统内存足够时它不会被回收。  
**弱引用**只要垃圾回收机制运行，就会回收内存。  
**虚引用**跟踪对象被垃圾回收的状态。

###**GC分代收集算法**  
**新生代-复制算法**  
**老年代-标记整理/清理算法**

###**分区收集算法**
**将整个堆空间划分成连续的不同小区间，每个小区间独立使用，独立回收**  

###**GC垃圾收集器**
**新生代**  
Serial(连续):复制算法,(client新生代默认垃圾收集器)  
ParNew(多线程)：复制算法，(client新生代默认垃圾收集器)  
Parallel(平行的) Scavenge：(关注程序达到一个可控制的吞吐量) 
G1：  
**老年代**  
CMS：多线程标记清除算法，获取最短垃圾回收停顿时间  
Serial Old(MSC)：单线程标记整理算法(Client默认的java虚拟机老年代垃圾回收器)  
Parallel Old：多线程标记整理算法(吞吐量优先)  
G1：(基于标记整理，不产生内存碎片，精确控制停顿时间)  

